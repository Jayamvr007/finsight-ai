# ─── LangChain / LangGraph Core ───────────────────────────────
langchain==0.3.25
langchain-community==0.3.24
langchain-google-genai==2.1.5
langgraph==0.2.74
langsmith==0.2.10

# ─── Persistence: PostgreSQL + LangGraph Checkpointer ─────────
langgraph-checkpoint-postgres>=0.1.0
psycopg[binary,pool]>=3.2.0          # psycopg3 — required by langgraph-checkpoint-postgres
psycopg2-binary>=2.9.9               # psycopg2 — used by SQLAlchemy sync ORM
sqlalchemy>=2.0.0                    # ORM for future PGVector migration

# ─── Vector Database ──────────────────────────────────────────
chromadb==0.6.3
pgvector>=0.2.5                      # PGVector Python client

# ─── Web Framework ────────────────────────────────────────────
fastapi==0.115.9
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
pydantic==2.10.0
python-multipart==0.0.20             # File upload support
sse-starlette==2.1.3

# ─── Async Task Queue ─────────────────────────────────────────
celery[redis]>=5.4.0                 # Celery with Redis broker
redis>=5.0.0                         # Redis Python client

# ─── RAG / Embeddings ─────────────────────────────────────────
sentence-transformers==3.3.1
pypdf==5.1.0
duckduckgo-search==7.5.0

# ─── Data & Finance ───────────────────────────────────────────
yfinance>=1.3.0
httpx==0.27.0
